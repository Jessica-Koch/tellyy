{
  "name": "tellyy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.0.1"
  },
  "engines": {
    "node": "10.9.0"
  },
  "scripts": {
    "install": "(cd client && yarn install) && (cd server && npm install)",
    "build": "concurrently \"cd client && yarn build\" \"cd server npm build\"",
    "heroku-postbuild": "yarn build",
    "dev": "concurrently \"cd client yarn start\" \"cd server npm start\"",
    "start": "cd server && npm start"
  }
}
